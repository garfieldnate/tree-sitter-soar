================================================================================
UnitTests/SoarTestAgents/testoutputleak.soar
================================================================================

sp {create-output
(state <s> ^io.output-link <ol>)
-->
(<ol> ^command.foo bar)
}

# Adding this production removes the leak
#sp {remove-output
#:o-support
#(state <s> ^io.output-link <ol>)
#(<ol> ^command <c>)
#(<c> ^status complete)
#-->
#(<ol> ^command <c> -)
#}
--------------------------------------------------------------------------------

(source_file
  (production
    (prod_name)
    (condsForOneId
      (condType)
      (singleTest
        (variable))
      (attrValueTests
        (singleTest
          (string))
        (singleTest
          (string))
        (valueTest
          (singleTest
            (variable)))))
    (variableMutation
      (variable)
      (attrValueMake
        (string)
        (valueMake
          (floatConstant))
        (valueMake
          (string))
        (valueMake
          (string))))))
