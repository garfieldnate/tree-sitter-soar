================================================================================
UnitTests/SoarTestAgents/FullTests/pref-semantics-test/three-way-two.soar
================================================================================

# A < B, A < C, B < C, C < B

sp {pref-semantics-test*propose*three-way-two
   (state <s> ^name pref-semantics-test
             -^three-way-two)
-->
   (<s> ^operator <op> + =)
   (<op> ^name three-way-two)
}

--------------------------------------------------------------------------------

(source_file
  (production
    (prod_name)
    (condsForOneId
      (condType)
      (singleTest
        (variable))
      (attrValueTests
        (singleTest
          (string))
        (valueTest
          (singleTest
            (string))))
      (attrValueTests
        (singleTest
          (string))))
    (variableMutation
      (variable)
      (attrValueMake
        (string)
        (valueMake
          (variable)
          (preferenceSpecifier
            (unaryPreference))
          (preferenceSpecifier
            (unaryPreference
              (unaryOrBinaryPreference))))))
    (variableMutation
      (variable)
      (attrValueMake
        (string)
        (valueMake
          (string))))))
